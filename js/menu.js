$(document).ready(function() {
    // all custom jQuery will go here
});

// gets the day of the week
function getDayofWeek() {
  let day = new Date().getDay();
  return day - 1;
}

// gets lunch for that day
function getLunch(day) {
  return null;
}

// gets lunch for that day
function getDinner(day) {
  return null; 
}
